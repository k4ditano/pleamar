// expect: error «a group with effects inside another group with effects»
scene X {
    surface { size: 100, 100 }
    group {
        blur: 4
        group {
            glow: 8, 50%
            ellipse { at: 50, 50; radius: 10; color: #fff }
        }
    }
}
