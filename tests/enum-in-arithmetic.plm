// expect: error «is an enum (low, normal, critical): you do not do arithmetic with it»
scene X {
    surface { size: 10, 10 }
    fact mode: low | normal | critical = low
    box { at: 5, 5; size: 4 + mode * 2, 4; color: #fff }
}
