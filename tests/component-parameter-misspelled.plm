// expect: error «'Row' has no parameter called 'tome'. Did you mean 'tone'?»
scene X {
    surface { size: 100, 100 }
    component Row(tone: color = #fff) { size: 10, 10; box { size: 10, 10; color: tone } }
    row { at: 0, 0; Row(tome: #000) }
}
