// espera: fallo ««fast» no es un valor de «mode»»
scene X {
    surface { size: 10, 10 }
    fact mode: low | normal | critical = low
    fact speed: slow | fast = slow
    box b { at: 5, 5; size: 4, 4; color: #fff }
    on press b { mode = fast }
}
