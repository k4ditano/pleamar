// espera: fallo «'fast' is not a value of 'mode': it can be low, normal or critical»
scene X {
    surface { size: 10, 10 }
    fact mode: low | normal | critical = low
    fact speed: slow | fast = slow
    box { at: 5, 5; size: 4, 4; color: #fff; opacity: mode == fast }
}
